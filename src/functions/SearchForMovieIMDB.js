import IMDBSearchRequest from './IMDBSearchRequest';

async function SearchForMovieIMDB(movie) {
	// let wikipedia_data = await IMDBSearchRequest(window.MovieGuru.GlobalData.IMDBSearchURL.replace('<MOVIETITLE>', encodeURIComponent(movie.name)));
	// console.log(wikipedia_data);
	let found_imdb = undefined;
	return found_imdb;
}

export default SearchForMovieIMDB;
